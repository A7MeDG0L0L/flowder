name: flowder_ex
description: Persistent file downloader manager, start, pause, resume, cancel your download.
version: 0.2.2
homepage: https://github.com/A7MeDG0L0L/flowder

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  dio: ^5.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter